/**
 * Public Process authoring contracts will be introduced here from executable
 * specifications. The empty module keeps the package boundary real without
 * inventing an API before those contracts exist.
 */
export {}

